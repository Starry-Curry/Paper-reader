---
name: paper-to-course
description: 将单篇学术论文转换为一套面向初学者的中文阅读辅导材料，默认生成导师式讲解 index.html、左侧详解右侧英文原文的 guided-reader.html、按论文结构翻译的 translated-paper.html，以及简要独立的 paper-notes.md。用于精读论文、逐步解释算法与公式、梳理训练和推理流程、制作中文全文译文和沉淀摘要式阅读笔记。
---

# Paper to Course

围绕“让没有相关背景的读者也能沿着一条主线读懂论文”生成四项互补产物。不要扩张为审稿、复现、文献综述或科研管理系统，也不要让固定模板取代论文自身的论证主线。

## 默认输入

至少读取一种可靠论文来源：

- 本地 PDF；
- arXiv、DOI 或论文网页；
- LaTeX 源码或用户提供的正文。

同时吸收用户明确给出的：

- 希望重点理解的问题；
- 已有背景与阅读目的；
- 输出语言、篇幅或风格偏好。

论文正文与用户预期明显冲突、关键页面无法读取或版本无法确认时才暂停询问；否则继续并明确标注不确定处。

## 默认输出

每次生成：

- `index.html`：导师式中文讲解，围绕问题、方法、公式、实验和边界组织。
- `guided-reader.html`：左侧是逐段、逐公式的零基础导师详解，右侧是与之对齐的英文原文。
- `translated-paper.html`：按原论文章节顺序制作的中文翻译，不改写成摘要。
- `paper-notes.md`：简要、独立、适合长期保存的摘要式阅读笔记。
- `assets/figures/`：四项产物共用的原论文图表、裁剪图或必要教学图。

现阶段不默认生成：

- PPTX；
- 额外的中英逐句翻译对照页；
- 审稿报告；
- 复现计划；
- 论文中间表示或证据账本；
- 多论文综述与知识库。

除非用户另行提出，不要因为目录中已有旧脚本或旧产物而创建或更新这些文件。

## 必读参考

按以下顺序读取：

1. 制作讲解前读取 [references/explanation-design.md](references/explanation-design.md)。
2. 论文包含模型、算法或关键公式时读取 [references/method-explanation.md](references/method-explanation.md)。
3. 论文包含关键公式时必须读取 [references/formula-tutorial.md](references/formula-tutorial.md)。
4. 制作中文译文前读取 [references/translation-guide.md](references/translation-guide.md)。
5. 制作双栏导师阅读器前读取 [references/guided-reader.md](references/guided-reader.md)。
6. 编写摘要笔记前读取 [references/reading-notes.md](references/reading-notes.md)。
7. 只有确实需要互动组件时才读取 [references/paper-elements.md](references/paper-elements.md)；其中内容不是必做清单。

## 工作流

### 1. 读完整篇论文

先核对：

- 标题、作者、机构、会议或版本；
- 摘要、引言、相关工作和结论；
- 方法、公式、算法、训练与推理；
- 数据集、基线、指标、主结果和消融；
- 局限、失败案例、附录与实现细节。

为关键内容保留 `§3.2`、`p.7`、`Fig. 4`、`Table 2`、`Eq. (6)` 等来源定位。不要只依据摘要生成完整讲解。

### 2. 确定一句讲解主线

除非用户明确说明有专业背景，默认读者不知道论文领域里的专用概念。在写页面前先列出“读者最终要回答的 3–6 个问题”和必要先修概念，再用一句话说明应该怎样带读者读懂论文，例如：

> 先区分 VAE、扩散过程和 DiT 主干，再分别走完训练与采样，最后用规模化实验检查论文的核心结论。

主线应回答：

1. 论文解决什么问题？
2. 旧方法为什么不够？
3. 新方法的核心洞察是什么？
4. 方法具体怎样运行？
5. 实验真正支持什么？
6. 结论的适用边界是什么？

模块数量、顺序和图示类型服从这条主线，不固定为六章，不强制时间线、对话或测验。

讲解顺序优先采用“为什么需要 → 最小直觉 → 正式定义 → 公式如何算 → 算法如何跑 → 实验是否支持”，不要直接照搬论文章节目录。

### 3. 准备共用素材

优先提取并复用：

- 最能说明方法的架构图；
- 最能支持结论的结果图或表；
- 关键公式；
- 必要的附录实现信息。

原图过于复杂时可以制作教学流程图、层级图或递进图，但必须标记为“教学图”。同一事实在四项产物中使用相同数字和来源。

### 4. 编写导师式讲解 `index.html`

讲解至少覆盖问题、方法、证据和边界。每一节优先采用：

```text
先给结论
→ 再解释机制
→ 再展示公式、流程或图表
→ 最后说明证据和限制
```

方法型论文必须把适用的下列内容讲清楚：

- 系统边界；
- 输入、输出和关键中间状态；
- 一个具体配置下的张量形状；
- 一次训练迭代；
- 一次推理或采样；
- 关键公式在流程中的位置；
- 原论文公式与教学化重写的区别。

理论、系统或综述类论文按自身逻辑替换，不要强行加入不适用的训练流程。

清楚区分：

- `论文报告`；
- `导师解读`；
- `延伸推断`。

关键公式必须先画出依赖关系，再区分：

- `锚点公式`：理解论文方法不可绕过，必须完整精讲；
- `桥接公式`：解释它怎样连接两个锚点；
- `记账公式`：说明用途和符号即可。

每个锚点公式遵守 `formula-tutorial.md`：先说它解决的问题，补齐先修概念，逐个解释符号、输入输出和读法，给可手算的最小数值例子或极端情形，再给不跳步的推导阶梯、训练或采样位置以及常见误解。不能用“显然”“类似可得”代替关键推导。

### 5. 制作双栏导师阅读器 `guided-reader.html`

从 `assets/guided-reader-template/` 复制模板和 CSS。按语义单元对齐：

- 左栏 `导师详解`：用零基础语言解释右栏这一段在回答什么、怎样接上主线；公式段还要说明读法、最小例子、算法位置和误解。
- 右栏 `英文原文`：保留对应的英文段落、公式、图表说明和来源定位，不放中文译文。

双栏页至少覆盖摘要与问题、方法主干、所有锚点公式、训练与推理、关键实验和结论边界。它不是把整篇讲解页与 PDF 机械并排，也不能只给一句摘要式批注。窄屏时一组内容上下堆叠，但语义对应关系必须保留。

### 6. 制作中文翻译 `translated-paper.html`

从 `assets/translation-template/` 复制模板并填充内容。保持：

- 原章节层级和顺序；
- 段落论证关系；
- 公式、编号和符号；
- 图表及其标题、图注和表注；
- 引用编号；
- 附录层级。

翻译不能压缩成摘要，也不能用讲解文案替代原文。专业术语首次出现时可写成“中文（English）”，后续保持统一。

参考文献书目信息可以保留原文，但必须在页面中说明处理方式。若无法完成全文翻译，不得把页面标成“全文翻译”；应继续补齐或明确报告缺失范围。

### 7. 编写简要笔记 `paper-notes.md`

从论文证据重新组织，而不是从 HTML 自动转换。默认保持简洁，只保留：

- 一页速览；
- 核心贡献；
- 方法与必要实现；
- 最关键实验结果；
- 局限与可选启发。

删除教学铺垫、重复类比、逐屏导航和大段翻译。只保留真正帮助日后回忆论文的图、表和公式。

### 8. 构建与检查

默认检查四项阅读产物：

```bash
node scripts/build-all.js <course-dir>
```

按需单独检查：

```bash
node scripts/build-all.js <course-dir> --html
node scripts/build-all.js <course-dir> --translation
node scripts/build-all.js <course-dir> --guided
node scripts/build-all.js <course-dir> --notes
node scripts/validate-notes.js <course-dir>/paper-notes.md
node scripts/validate-translation.js <course-dir>/translated-paper.html
node scripts/validate-guided-reader.js <course-dir>/guided-reader.html
```

没有显式 `--pptx` 时不得创建或更新 `slides.pptx`。现阶段默认流程不运行 PPTX。

## 完成标准

- 四项默认产物全部存在且可独立阅读。
- HTML 讲解有明确主线，视觉表达确实降低理解成本。
- 方法型论文把训练、推理、张量和公式关系讲清楚；初学者能手算至少一个最小例子并沿推导阶梯解释锚点公式。
- 双栏页左侧是足量的导师详解，右侧是对齐的英文原文；每一组都有来源定位，窄屏不会失去对应关系。
- 中文翻译保留原结构和论证，不被改写成摘要。
- Markdown 笔记简短但能复述问题、方法、结果和局限。
- 精确数字同时说明任务、模型、指标、比较对象和来源。
- 原论文公式、等价解释和教学化重写不会混淆。
- 图片与本地资源无断链、占位符和远程字体依赖。
- 默认构建不生成或修改 PPTX。
